{
  "name": "verbugata-api",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  },
  "scripts": {
    "build": "npx tsc --build tsconfig.json",
    "start": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "fastify": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.3.3",
    "typescript": "^5.1.6"
  }
}
